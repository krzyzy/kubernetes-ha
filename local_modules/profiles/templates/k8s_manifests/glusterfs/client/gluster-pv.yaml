apiVersion: "v1"
kind: "PersistentVolume"
metadata:
  name: "media-storage"
spec:
  capacity:
    storage: "2Gi"
  accessModes:
    - ReadWriteMany
  glusterfs:
    endpoints: "glusterfs-cluster"
    path: "/mnt/brick1/media-storage"
    readOnly: false
  persistentVolumeReclaimPolicy: "Recycle"
